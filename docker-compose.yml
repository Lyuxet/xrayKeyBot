version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: keybot
    restart: unless-stopped
    working_dir: /app
    command: python bot.py
    env_file:
      - .env
    volumes:
      - .:/app
    tty: true